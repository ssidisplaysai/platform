# {{EntityName}} Documentation

> This is a Phase 5 generated documentation template for {{EntityName}}.
>
> Generated at {{GeneratedAt}}

## Overview

{{EntityName}} is a business entity that represents...

**Phase 5 Template:** This is a placeholder documentation file. Fill in the actual entity description and documentation here.

## Entity Structure

The {{EntityName}} entity contains the following core fields:

- `id` (UUID) - Unique identifier
- `createdAt` (Date) - Creation timestamp
- `updatedAt` (Date) - Last update timestamp

## Operations

### Create {{EntityName}}

```typescript
import { {{entityNameLower}}Service } from "@/core/services/{{EntityName}}Service";

const {{entityNameLower}} = await {{entityNameLower}}Service.create({
  // Entity data here
});
```

### Retrieve {{EntityName}}

```typescript
const {{entityNameLower}} = await {{entityNameLower}}Service.get(id);
```

### Update {{EntityName}}

```typescript
const updated = await {{entityNameLower}}Service.update(id, {
  // Updated fields here
});
```

### Delete {{EntityName}}

```typescript
await {{entityNameLower}}Service.delete(id);
```

## Searching

```typescript
import { {{entityNameLower}}SearchEngine } from "@/core/search/{{EntityName}}Search";

const results = await {{entityNameLower}}SearchEngine.search({
  q: "search query",
  limit: 10,
  offset: 0,
});
```

## Permissions

{{EntityName}} operations are controlled by permissions:

- `{{entityNameLower}}:view` - View {{EntityName}} entities
- `{{entityNameLower}}:create` - Create new {{EntityName}}
- `{{entityNameLower}}:update` - Update existing {{EntityName}}
- `{{entityNameLower}}:delete` - Delete {{EntityName}}
- `{{entityNameLower}}:export` - Export {{EntityName}} data
- `{{entityNameLower}}:admin` - Administrative access

## Events

{{EntityName}} operations emit the following events:

- `{{EntityName}}.created` - When a new {{EntityName}} is created
- `{{EntityName}}.updated` - When a {{EntityName}} is updated
- `{{EntityName}}.deleted` - When a {{EntityName}} is deleted

## Validation

{{EntityName}} validation is performed by the {{EntityName}}Validator class.

**Phase 5 Template:** Validation rules are not implemented yet.

## API Reference

### Service Methods

- `get(id)` - Get a {{EntityName}} by ID
- `list()` - List all {{EntityName}} entities
- `search(query)` - Search {{EntityName}} entities
- `create(data)` - Create a new {{EntityName}}
- `update(id, data)` - Update a {{EntityName}}
- `delete(id)` - Delete a {{EntityName}}

### Repository Methods

- `findById(id)` - Find by ID
- `findAll()` - Find all entities
- `findByFilter(filter)` - Find by filter
- `create(data)` - Create entity
- `update(id, data)` - Update entity
- `delete(id)` - Delete entity
- `count()` - Count entities

## Related Entities

**Phase 5 Template:** Related entities would be documented here.

## Notes

This documentation was generated by the Genesis Business Compiler (Phase 5).
Actual implementations are pending.
