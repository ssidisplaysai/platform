# Dallas Rich Composition Apply V1

## Scope

This workflow applies only the exact owner-approved Dallas V3 composition to WordPress page `13084`. It does not authorize publication, campaign transition, dispatch, another geography, another page, or another site.

## Authority map

Authenticated WordPress inspection established:

- Operative body: `WORDPRESS_POST_CONTENT`
- Shadow authority: none
- Elementor document: absent
- Template/header/footer: existing WordPress theme
- SEO: Yoast REST authority
- Media: WordPress media library
- Parent: `13083`
- Status: `draft`

The apply operation changes only the operative body and uploads the three approved generated V3 assets. It does not update global templates, Yoast values, page hierarchy, title, slug, featured media, campaign records, or publication state.

## Exact approval

The append-only approval binds the owner statement `yes its great` to:

- organization, site, campaign, target, job, and page revision;
- WordPress object `13084`;
- V3 preview evidence manifest and hash;
- localized V2 bundle, context, link graph, theme, application authority, and composition plan;
- market bundle, intelligence, and page-strategy hash;
- semantic media package hash.

Any identity mismatch fails before WordPress mutation.

## Transaction

1. Re-read the exact WordPress draft and authority map.
2. Persist owner approval and an exact rollback artifact.
3. Re-verify product media bytes and intended links.
4. Upload the frozen contextual, application, and atmospheric images with non-documentary metadata.
5. Render the approved composition as scoped, self-contained WordPress HTML.
6. Update only object `13084` through the existing draft-only writer.
7. Read back title, slug, parent, status, content, H1, featured media, and SEO.
8. Compare semantic structure and authority against the approved V3 input.
9. Persist the mutation receipt and audit trail.

If a post-write check fails, the workflow restores the exact captured `post_content` and removes newly uploaded media. Successful application leaves the rollback artifact available and unused.

## Media safety

- `PRODUCT_AUTHORITY`: existing approved media `10757`.
- `CONTEXTUAL_IN_USE`: conceptual, product-grounded visualization.
- `APPLICATION_EXPERIENCE`: projection-mapping application visualization.
- `LOCAL_CONTEXTUAL_ATMOSPHERE`: atmospheric North Texas visualization.

Generated media metadata states that it is conceptual and not documentary evidence of a Dallas installation, customer, office, or completed SSI project.

## Capture and drift

The existing governed authenticated draft-snapshot architecture captures operative WordPress content at 1440, 1024, 768, and 375 pixels. Artifacts are immutable and hash-verified.

The capture does not embed global WordPress theme chrome. Header alignment is therefore `NOT_EVALUATED`, and this bounded shell difference produces `MINOR` drift. Section sequence, semantic media, CTA hierarchy, link graph, local theme, and projection-mapping emphasis must match. `MATERIAL` or `CRITICAL` drift blocks completion and never authorizes publication.

## Owner review

The comparison route is evidence-only. It has no Publish control. The owner must review actual WordPress draft captures before any separate publication authorization is considered.
