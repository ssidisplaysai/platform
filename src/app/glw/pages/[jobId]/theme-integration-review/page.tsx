import { notFound } from "next/navigation";import { AppShell } from "@/components/layout/app-shell";import { GlwDallasThemeIntegrationReview } from "@/modules/glw/GlwDallasThemeIntegrationReview";import { getDallasPublicationState } from "@/modules/glw/dallas-reference-page-publication-repository";import { getDallasThemeIntegrationState } from "@/modules/glw/dallas-public-theme-integration-repository";
export const dynamic="force-dynamic";export const revalidate=0;type Props={params:Promise<{jobId:string}>;searchParams:Promise<{organizationId?:string|string[];siteId?:string|string[]}>};const first=(value:string|string[]|undefined)=>typeof value==="string"?value:value?.[0]??null;export default async function DallasThemeIntegrationReviewPage({params,searchParams}:Props){const [{jobId},query]=await Promise.all([params,searchParams]);if(jobId!=="2ca74016-252b-4587-bf3c-ec9b7eb839c9"||first(query.organizationId)!=="ssi"||first(query.siteId)!=="site-ssi-projectorenclosure")notFound();const publication=getDallasPublicationState();const integration=getDallasThemeIntegrationState();const before=publication.visualCertifications.at(-1),receipt=integration.receipts.at(-1),after=integration.certifications.at(-1);if(!before||!receipt||!after)notFound();return <AppShell><GlwDallasThemeIntegrationReview before={before} receipt={receipt} after={after}/></AppShell>}