import { NextRequest } from "next/server";
jest.mock("@/modules/foundation/rendered-visual-certification-repository",()=>({getRenderedVisualCertificationById:jest.fn(),decideRenderedVisualCertification:jest.fn()}));
import { decideRenderedVisualCertification, getRenderedVisualCertificationById } from "@/modules/foundation/rendered-visual-certification-repository";
import { POST } from "../route";
const getCertification=jest.mocked(getRenderedVisualCertificationById);const decide=jest.mocked(decideRenderedVisualCertification);const context={params:Promise.resolve({certificationId:"cert"})};
const identity={organizationId:"o",siteId:"s",pageId:"p",pageRevisionIdentity:"r",canonicalPath:"/",contentHash:"a".repeat(64),renderedContentHash:"b".repeat(64),campaignId:null,targetId:null,jobId:null,externalExecutionId:null,wordpressObjectId:"1",wordpressStatus:"publish"};
function request(body:unknown,role="platform_admin"){return new NextRequest("http://localhost/api",{method:"POST",headers:{"content-type":"application/json","x-gcp-roles":role,"x-gcp-organization-id":"o","x-gcp-site-id":"s"},body:JSON.stringify(body)});}
describe("visual certification decision route",()=>{beforeEach(()=>jest.clearAllMocks());it("requires platform admin and exact APPROVED operation",async()=>{expect((await POST(request({operation:"DECIDE_VISUAL_CERTIFICATION",decision:"APPROVED"},"ops_manager"),context)).status).toBe(403);expect((await POST(request({operation:"DECIDE_VISUAL_CERTIFICATION",decision:"NEEDS_FIX"}),context)).status).toBe(400);});it("binds approval to exact certification identity without publication authority",async()=>{getCertification.mockReturnValueOnce({certificationId:"cert",identity} as never);decide.mockReturnValueOnce({decisionId:"d",certificationId:"cert",decision:"APPROVED",publicationAuthorized:false} as never);const response=await POST(request({operation:"DECIDE_VISUAL_CERTIFICATION",decision:"APPROVED"}),context);expect(response.status).toBe(201);expect(decide).toHaveBeenCalledWith(expect.objectContaining({certificationId:"cert",decision:"APPROVED",currentIdentity:identity}));expect(await response.json()).toMatchObject({publicationAuthorized:false,publicationPerformed:false,wordpressMutationPerformed:false,campaignMutationPerformed:false,activationPerformed:false});});});