import { NextRequest,NextResponse } from "next/server";import { authorizeRequest,resolveRequestScope } from "@/modules/foundation/api-auth";import { captureDallasActualWordPress } from "@/modules/glw/dallas-actual-wordpress-capture-service";
export const dynamic="force-dynamic";export const maxDuration=120;
export async function POST(request:NextRequest,context:{params:Promise<{jobId:string}>}){const auth=authorizeRequest(request,"sites:update");if(!auth.ok)return NextResponse.json({error:auth.error},{status:auth.status});const scope=resolveRequestScope(request);const {jobId}=await context.params;const body=await request.json().catch(()=>null) as {operation?:string}|null;if(!auth.roles.includes("platform_admin")||scope.organizationId!=="ssi"||scope.siteId!=="site-ssi-projectorenclosure"||jobId!=="2ca74016-252b-4587-bf3c-ec9b7eb839c9"||body?.operation!=="CAPTURE_ACTUAL_DALLAS_DRAFT")return NextResponse.json({error:"Exact Dallas capture authority required."},{status:403});try{const result=await captureDallasActualWordPress();return NextResponse.json({...result,wordpressMutationPerformed:false,campaignMutationPerformed:false,publicationPerformed:false,dispatchPerformed:false},{status:result.reused?200:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message.split(":")[0]:"DALLAS_CAPTURE_FAILED",wordpressMutationPerformed:false,campaignMutationPerformed:false,publicationPerformed:false,dispatchPerformed:false},{status:422})}}