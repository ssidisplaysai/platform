"use client";

import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";

export default function OperatorLoginPage() { const router = useRouter(); const [error, setError] = useState<string | null>(null); const [pending, setPending] = useState(false); useEffect(() => { let active = true; void fetch("/api/operator-session", { cache: "no-store" }).then(async (response) => { if (!active || !response.ok) return; router.replace("/glw"); router.refresh(); }).catch(() => undefined); return () => { active = false; }; }, [router]); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setError(null); const data = new FormData(event.currentTarget); const response = await fetch("/api/operator-session", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ identity: data.get("identity"), password: data.get("password") }) }); setPending(false); if (!response.ok) { setError("Authentication failed."); return; } router.replace("/glw/campaigns"); router.refresh(); } return <main className="mx-auto flex min-h-screen max-w-md items-center px-6"><form onSubmit={submit} className="w-full space-y-5"><div><p className="text-sm font-semibold uppercase">Genesis Operator</p><h1 className="text-3xl font-semibold">Sign in</h1></div><label className="block">Operator identity<input className="mt-2 w-full border p-3" name="identity" autoComplete="username" required /></label><label className="block">Password<input className="mt-2 w-full border p-3" name="password" type="password" autoComplete="current-password" required /></label>{error ? <p role="alert">{error}</p> : null}<button className="w-full bg-black p-3 text-white" disabled={pending} type="submit">{pending ? "Signing in" : "Sign in"}</button></form></main>; }