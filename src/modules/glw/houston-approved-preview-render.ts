import { createHash } from "node:crypto";

import type { LocalPageThemingBundle } from "@/modules/foundation/local-context-page-theming-repository";
import type { MarketProductMatchBundle } from "@/modules/foundation/local-market-product-match-repository";
import { HOUSTON_H1 } from "./houston-approved-preview-draft";
import { HOUSTON_NARRATIVE } from "./houston-reference-preview";

const esc = (value: string) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
export const hashHoustonDraftContent = (value: string) => createHash("sha256").update(value).digest("hex");

export function renderApprovedHoustonWordPress(input: { local: LocalPageThemingBundle; market: MarketProductMatchBundle; mediaUrls: Record<"PRODUCT_AUTHORITY" | "CONTEXTUAL_IN_USE" | "APPLICATION_EXPERIENCE" | "LOCAL_CONTEXTUAL_ATMOSPHERE", string> }) {
  const links = new Map(input.local.links.links.map((item) => [item.linkId, item]));
  const link = (id: string) => { const item = links.get(id); if (!item) throw new Error(`HOUSTON_DRAFT_LINK_REQUIRED:${id}`); return `<a href="${esc(item.url)}">${esc(item.anchorIntent)}</a>`; };
  const selected = input.market.opportunities.filter((item) => item.confidence === "HIGH" || item.confidence === "MEDIUM");
  if (input.market.pageStrategy.primaryApplication !== "COMMERCIAL_AV" || input.local.media.length !== 4) throw new Error("HOUSTON_DRAFT_STRATEGY_STALE");
  return `<!-- wp:html --><style>.ghd{--ink:#14201f;--red:#b4251d;--gold:#f1b84b;--green:#17614c;--paper:#eef1ee;color:var(--ink);font-family:Arial,sans-serif;margin:0}.ghd *{box-sizing:border-box}.ghd-wrap{width:min(1180px,calc(100% - 40px));margin:auto}.ghd a{color:inherit}.ghd-hero{min-height:680px;display:flex;align-items:flex-end;padding:80px 0;background:linear-gradient(90deg,rgba(16,33,29,.96),rgba(16,33,29,.76) 52%,rgba(16,33,29,.3)),url('${esc(input.mediaUrls.LOCAL_CONTEXTUAL_ATMOSPHERE)}') center/cover;color:#fff}.ghd-kicker{color:#7dc7ad;font-size:12px;font-weight:900;text-transform:uppercase}.ghd h1,.ghd h2{font-family:Impact,'Arial Narrow',sans-serif;letter-spacing:0}.ghd h1{max-width:940px;margin:18px 0;font-size:clamp(44px,7vw,86px);line-height:.98}.ghd h2{font-size:clamp(34px,5vw,60px);line-height:1.02;margin:0 0 22px}.ghd-copy{max-width:720px;font-size:17px;line-height:1.7}.ghd-actions{display:flex;flex-wrap:wrap;gap:12px;margin-top:28px}.ghd-button{display:inline-flex;padding:15px 22px;background:var(--gold);color:var(--ink)!important;text-decoration:none;font-size:13px;font-weight:900;text-transform:uppercase}.ghd-button--outline{background:transparent;color:#fff!important;border:1px solid #fff}.ghd-note{margin-top:28px;font-size:11px;text-transform:uppercase;opacity:.65}.ghd-section{padding:80px 0}.ghd-product{display:grid;grid-template-columns:1.05fr .95fr;gap:56px;align-items:center}.ghd-product img{width:100%;height:auto;background:#edf0ed;padding:18px;border:1px solid #ccd3cf}.ghd-split{display:grid;grid-template-columns:1fr 1fr;background:var(--ink);color:#fff}.ghd-split img{width:100%;height:100%;min-height:500px;object-fit:cover}.ghd-split-copy{padding:70px;display:flex;flex-direction:column;justify-content:center}.ghd-application{min-height:680px;display:flex;align-items:flex-end;padding:80px 0;background:linear-gradient(0deg,rgba(13,23,22,.94),rgba(13,23,22,.24)),url('${esc(input.mediaUrls.APPLICATION_EXPERIENCE)}') center/cover;color:#fff}.ghd-planning{background:#dfe9e3}.ghd-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.ghd-card{background:#fff;border-top:3px solid var(--green);padding:22px}.ghd-warning{background:var(--gold);padding:48px 0}.ghd-opportunities{background:var(--ink);color:#fff}.ghd-opportunity-grid,.ghd-link-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1px}.ghd-opportunity,.ghd-link{padding:24px;border:1px solid rgba(255,255,255,.15)}.ghd-links{background:#fff}.ghd-link{display:block;background:#f5f6f3;color:var(--ink)!important;text-decoration:none}.ghd-local{background:#d8ded9}.ghd-cta{background:var(--red);color:#fff}.ghd-cta .ghd-wrap{display:flex;justify-content:space-between;gap:32px;align-items:end}@media(max-width:800px){.ghd-hero,.ghd-application{min-height:580px;padding:58px 0}.ghd-product,.ghd-split{grid-template-columns:1fr}.ghd-split-copy{padding:52px 24px}.ghd-grid,.ghd-opportunity-grid,.ghd-link-grid{grid-template-columns:1fr}.ghd-cta .ghd-wrap{display:block}.ghd-cta .ghd-button{margin-top:24px}}@media(max-width:430px){.ghd-wrap{width:calc(100% - 28px)}.ghd h1{font-size:42px}.ghd h2{font-size:34px}.ghd-section{padding:58px 0}}</style><main class="ghd" data-genesis-primary-content data-composition-version="HOUSTON_V1" data-market-primary="COMMERCIAL_AV"><section class="ghd-hero" data-genesis-hero data-media-role="LOCAL_CONTEXTUAL_ATMOSPHERE"><div class="ghd-wrap"><p class="ghd-kicker">${esc(HOUSTON_NARRATIVE.eyebrow)}</p><h1>${esc(HOUSTON_H1)}</h1><p class="ghd-copy">${esc(HOUSTON_NARRATIVE.heroBody)}</p><div class="ghd-actions"><a class="ghd-button" href="${esc(links.get("quote")!.url)}">Discuss your project</a><a class="ghd-button ghd-button--outline" href="${esc(links.get("product")!.url)}">Review the enclosure</a></div><p class="ghd-note">Atmospheric visualization · Houston familiarity without local-presence claims</p></div></section><section class="ghd-section"><div class="ghd-wrap ghd-product" data-media-role="PRODUCT_AUTHORITY"><img src="${esc(input.mediaUrls.PRODUCT_AUTHORITY)}" alt="Approved fan-cooled projector enclosure"><div><p class="ghd-kicker">The product</p><h2>${esc(HOUSTON_NARRATIVE.productHeading)}</h2><p class="ghd-copy">${esc(HOUSTON_NARRATIVE.productBody)}</p>${link("product")}</div></div></section><section class="ghd-split" data-media-role="CONTEXTUAL_IN_USE"><img src="${esc(input.mediaUrls.CONTEXTUAL_IN_USE)}" alt="Concept visualization of a fan-cooled projector enclosure in a covered Houston-relevant commercial AV concourse"><div class="ghd-split-copy"><p class="ghd-kicker">Product in context</p><h2>${esc(HOUSTON_NARRATIVE.inUseHeading)}</h2><p class="ghd-copy">${esc(HOUSTON_NARRATIVE.inUseBody)}</p><p class="ghd-note">Conceptual contextual · Product grounded · Not documentary evidence</p></div></section><section class="ghd-application" data-media-role="APPLICATION_EXPERIENCE"><div class="ghd-wrap"><p class="ghd-kicker">Evidence-selected application</p><h2>${esc(HOUSTON_NARRATIVE.applicationHeading)}</h2><p class="ghd-copy">${esc(HOUSTON_NARRATIVE.applicationBody)}</p><p class="ghd-note">Application visualization · Not a Houston venue, customer, or installation</p></div></section><section class="ghd-section ghd-planning"><div class="ghd-wrap"><p class="ghd-kicker">Houston environmental context</p><h2>${esc(HOUSTON_NARRATIVE.planningHeading)}</h2><p class="ghd-copy">${esc(HOUSTON_NARRATIVE.planningBody)}</p><div class="ghd-grid">${input.local.context.facts.slice(0,4).map((fact)=>`<article class="ghd-card"><strong>${esc(fact.kind.replaceAll("_"," "))}</strong><p>${esc(fact.statement)}</p></article>`).join("")}</div></div></section><section class="ghd-warning"><div class="ghd-wrap"><p class="ghd-kicker">Product suitability boundary</p><h2>${esc(HOUSTON_NARRATIVE.suitabilityHeading)}</h2><p class="ghd-copy">${esc(HOUSTON_NARRATIVE.suitabilityBody)}</p><div class="ghd-actions">${link("climate-controlled")} ${link("outdoor")}</div></div></section><section class="ghd-section ghd-opportunities"><div class="ghd-wrap"><p class="ghd-kicker">Houston market-product match</p><h2>Lead with commercial AV. Keep every application bounded by product truth.</h2><div class="ghd-opportunity-grid">${selected.map((item)=>`<article class="ghd-opportunity"><strong>${esc(item.confidence)} · ${esc(item.applicationId.replaceAll("_"," "))}</strong><p>${esc(item.whyProduct)}</p></article>`).join("")}</div></div></section><section class="ghd-section ghd-links"><div class="ghd-wrap"><p class="ghd-kicker">Useful pathways</p><h2>Move from Houston context to the right enclosure review.</h2><div class="ghd-link-grid">${input.local.links.links.filter((item)=>item.role.startsWith("INTERNAL")&&!["product","application"].includes(item.linkId)).map((item)=>`<a class="ghd-link" href="${esc(item.url)}"><strong>${esc(item.anchorIntent)}</strong><p>${esc(item.reason)}</p></a>`).join("")}</div></div></section><section class="ghd-section ghd-local"><div class="ghd-wrap"><p class="ghd-kicker">Houston and Southeast Texas planning resources</p><div class="ghd-link-grid">${input.local.links.links.filter((item)=>!item.role.startsWith("INTERNAL")).map((item)=>`<a class="ghd-link" href="${esc(item.url)}"><strong>${esc(item.anchorIntent)}</strong><p>${esc(item.reason)}</p></a>`).join("")}</div></div></section><section class="ghd-section ghd-cta"><div class="ghd-wrap"><div><p class="ghd-kicker">Project-specific review</p><h2>Bring the projector, lens, environment, duty cycle, and exposure plan.</h2><p class="ghd-copy">SSI can help determine whether fan-cooled, climate-controlled, IP-rated, or another enclosure direction fits the actual Houston-area project.</p></div><a class="ghd-button" href="${esc(links.get("quote")!.url)}">Request project review</a></div></section></main><!-- /wp:html -->`;
}

export function evaluateHoustonPreviewDraftDrift(input: { expectedBodyHash: string; actualBodyHash: string; expectedLinks: readonly string[]; actualLinks: readonly string[]; expectedRoles: readonly string[]; actualRoles: readonly string[]; actualHtml: string }) {
  const linksMatch = [...new Set(input.expectedLinks)].every((item) => input.actualLinks.includes(item));
  const rolesMatch = [...input.expectedRoles].sort().join("|") === [...input.actualRoles].sort().join("|");
  const strategyMatch = /data-market-primary="COMMERCIAL_AV"/.test(input.actualHtml) && /EVENT VENUE/i.test(input.actualHtml) && /PROJECTION MAPPING/i.test(input.actualHtml);
  const suitabilityMatch = /climate-controlled/i.test(input.actualHtml) && /humidity/i.test(input.actualHtml) && /covered/i.test(input.actualHtml);
  const claimSafe = !/(?:our|the) Houston (?:office|team|customer|installation)|installed at|completed (?:in|at) Houston/i.test(input.actualHtml);
  const critical = input.expectedBodyHash !== input.actualBodyHash || !rolesMatch;
  const material = !linksMatch || !strategyMatch || !suitabilityMatch || !claimSafe;
  const reasons = [...(input.expectedBodyHash === input.actualBodyHash ? [] : ["BODY_CHANGED"]), ...(rolesMatch ? [] : ["MEDIA_ROLES_CHANGED"]), ...(linksMatch ? [] : ["LINK_GRAPH_CHANGED"]), ...(strategyMatch ? [] : ["MARKET_STRATEGY_CHANGED"]), ...(suitabilityMatch ? [] : ["SUITABILITY_CHANGED"]), ...(claimSafe ? [] : ["CLAIM_SAFETY_FAILED"])];
  return { drift: critical ? "CRITICAL" as const : material ? "MATERIAL" as const : "MINOR" as const, reasons };
}
