jest.mock("server-only", () => ({}));

import { readFileSync } from "node:fs";
import { join } from "node:path";
import { HOUSTON_H1 } from "../houston-approved-preview-draft";
import { evaluateHoustonDraftPublicDrift, evaluateHoustonPublicationReadiness, evaluateHoustonPublicEvidence, evaluatePublicSeoContract, freezeHoustonPublicationIdentity, freezeHoustonRetryAuthority, freezeHoustonRetryV3Authority, normalizeHoustonSemanticText, resolveDallasCertifiedSeoContract, resolveHoustonPublicSeoContract, HOUSTON_APPROVED_BODY_HASH, HOUSTON_APPROVED_SEO_HASH, HOUSTON_H1_NORMALIZATION_REPAIR_COMMIT, HOUSTON_MEDIA_ROLES, HOUSTON_PUBLICATION_COMMIT, HOUSTON_PUBLIC_URL, HOUSTON_PUBLIC_VERIFIER_VERSION, HOUSTON_V1_APPROVAL_ID, HOUSTON_V1_FAILURE_REASON, HOUSTON_V1_INTENT_ID, HOUSTON_V1_RECEIPT_ID, HOUSTON_V2_APPROVAL_ID, HOUSTON_V2_FAILURE_ID, HOUSTON_V2_INTENT_ID, HOUSTON_V2_RECEIPT_ID, HOUSTON_V2_ROLLBACK_ID, HOUSTON_VERIFIER_REPAIR_COMMIT, HOUSTON_WORDPRESS_OBJECT_ID, type HoustonFirstPublicProbe, type HoustonPublicSeoContract } from "../houston-reference-page-publication";
import { collectHoustonFirstPublicProbe } from "../houston-reference-page-publication-service";

const approvalIdentity = { targetId: "target-campaign-ssi-site-ssi-projectorenclosure-fan-cooled-projector-enclosures-texas-cities-tx-houston", canonicalPath: "fan-cooled-projector-enclosures/texas/houston", previewCommit: "6cb18a49eb54ec412ea14ab9681e06f46d1659d2", localBundleId: "local-theming-houston-southeast-texas-v1", localContextId: "context", localLinkGraphId: "links", localThemeProfileId: "theme", applicationAuthorityId: "application", compositionPlanId: "composition", marketBundleId: "market-match-houston-southeast-texas-v1", marketIntelligenceId: "market", pageStrategyHash: "a".repeat(64), semanticMediaHash: "b".repeat(64), productMediaHash: "685495793be84b3a9d1a7e902087d63ae7a636e2042e6c0d592f5ba83e767078", previewVisualCertificationId: "preview-cert", previewVisualCertificationHash: "c".repeat(64), previewRoute: "/glw/houston-reference-preview" } as const;
const receipt = { receiptId: "draft-receipt", decisionId: "draft-approval", wordpressObjectId: HOUSTON_WORDPRESS_OBJECT_ID, bodyHash: HOUSTON_APPROVED_BODY_HASH, seoHash: HOUSTON_APPROVED_SEO_HASH, uploadedMedia: [{ role: "CONTEXTUAL_IN_USE", mediaId: 1, url: "https://projectorenclosure.com/a.jpg", sha256: "d".repeat(64), claimClass: "CONCEPTUAL" }] };
const themeEvidence = { visibleH1Count: 1, duplicateThemeTitleVisible: false, duplicateThemeFeaturedMediaVisible: false };
const certification = { certificationId: "theme-cert", receiptId: "draft-receipt", themeIntegrationReady: true, overallState: "PASS", captures: [1440, 1024, 768, 375].map(() => ({ horizontalOverflow: 0, themeIntegration: themeEvidence })) };
const comparison = { comparisonId: "comparison", receiptId: "draft-receipt", draftVisualCertificationId: "theme-cert", drift: "MINOR" };
const readback = { wordpressObjectId: HOUSTON_WORDPRESS_OBJECT_ID, status: "draft", parent: 13083, title: "Fan Cooled Projector Enclosures in Houston", slug: "houston", bodyHash: HOUSTON_APPROVED_BODY_HASH, seoHash: HOUSTON_APPROVED_SEO_HASH, featuredMediaId: 10757, template: "elementor_header_footer", hideTitle: "yes", roles: HOUSTON_MEDIA_ROLES };
const freeze = (patch: Record<string, unknown> = {}) => freezeHoustonPublicationIdentity({ receipt: receipt as never, approvalIdentity, certification, comparison, readback, repositoryCommit: HOUSTON_PUBLICATION_COMMIT, ...patch });
const probe = (patch: Partial<HoustonFirstPublicProbe> = {}): HoustonFirstPublicProbe => ({ probeId: "probe", publicationIntentId: "intent", publicationReceiptId: "publication", wordpressObjectId: HOUSTON_WORDPRESS_OBJECT_ID, requestedUrl: HOUSTON_PUBLIC_URL, startedAt: "2026-09-13T20:28:48.000Z", completedAt: "2026-09-13T20:28:49.000Z", httpStatus: 200, httpClassification: "HTTP_200", finalUrl: HOUSTON_PUBLIC_URL, redirectChain: [], responseHeaders: { contentType: "text/html", cacheControl: "no-cache", server: "Apache", age: null, via: null, xCache: null }, contentType: "text/html", contentHash: "a".repeat(64), title: "Fan Cooled Projector Enclosures Houston | ProjectorEnclosure.com", metaDescription: "Plan fan cooled projector enclosures for Houston commercial AV, covered event, and convention spaces with project-specific exposure review.", metaDescriptionPresent: true, openGraphDescription: "Houston projector enclosure planning.", canonical: HOUSTON_PUBLIC_URL, robots: "index, follow", indexability: "INDEXABLE", visibleH1Count: 1, visibleH1Texts: [HOUSTON_H1], normalizedH1Texts: [normalizeHoustonSemanticText(HOUSTON_H1)], expectedH1Raw: HOUSTON_H1, expectedH1Normalized: normalizeHoustonSemanticText(HOUSTON_H1), bodyMarkerClassification: "APPROVED_CONTENT", approvedContentMarkerPresent: true, semanticMediaRoles: HOUSTON_MEDIA_ROLES, devLeak: false, previewLeak: false, networkErrorClass: null, verifierVersion: HOUSTON_PUBLIC_VERIFIER_VERSION, ...patch });
const evaluate = (inputProbe = probe(), seoContract = resolveHoustonPublicSeoContract()) => evaluateHoustonPublicEvidence({ probe: inputProbe, seoContract, linksValid: true, mediaValid: true, claimSafe: true });

describe("Houston reference-page publication contract", () => {
  test("binds V2 retry authority to the exact V1 failure, rollback, repair, and verifier", () => { const authority = freezeHoustonRetryAuthority({ priorApprovalId: HOUSTON_V1_APPROVAL_ID, priorIntentId: HOUSTON_V1_INTENT_ID, priorPublicationReceiptId: HOUSTON_V1_RECEIPT_ID, priorFailureId: "failure-v1", priorFailureReason: HOUSTON_V1_FAILURE_REASON, priorRollbackVerified: true, previousDraftApprovalId: "draft-approval", verifierRepairCommit: HOUSTON_VERIFIER_REPAIR_COMMIT, seoContractVersion: "projectorenclosure-public-seo-v1", publicVerifierVersion: HOUSTON_PUBLIC_VERIFIER_VERSION }); expect(authority).toMatchObject({ attempt: "V2", priorFailureId: "failure-v1", priorRollbackVerified: true, verifierRepairCommit: HOUSTON_VERIFIER_REPAIR_COMMIT }); });
  test.each([{ priorApprovalId: "wrong" }, { priorRollbackVerified: false }, { verifierRepairCommit: "stale" }, { seoContractVersion: "wrong" }, { publicVerifierVersion: "wrong" }])("rejects stale V2 retry authority", (patch) => expect(() => freezeHoustonRetryAuthority({ priorApprovalId: HOUSTON_V1_APPROVAL_ID, priorIntentId: HOUSTON_V1_INTENT_ID, priorPublicationReceiptId: HOUSTON_V1_RECEIPT_ID, priorFailureId: "failure-v1", priorFailureReason: HOUSTON_V1_FAILURE_REASON, priorRollbackVerified: true, previousDraftApprovalId: "draft-approval", verifierRepairCommit: HOUSTON_VERIFIER_REPAIR_COMMIT, seoContractVersion: "projectorenclosure-public-seo-v1", publicVerifierVersion: HOUSTON_PUBLIC_VERIFIER_VERSION, ...patch })).toThrow("HOUSTON_V2_RETRY_AUTHORITY_STALE"));
  test("binds V3 retry authority to the exact V2 H1 failure and normalization repair", () => expect(freezeHoustonRetryV3Authority({ priorApprovalId: HOUSTON_V2_APPROVAL_ID, priorIntentId: HOUSTON_V2_INTENT_ID, priorPublicationReceiptId: HOUSTON_V2_RECEIPT_ID, priorFailureId: HOUSTON_V2_FAILURE_ID, priorRollbackId: HOUSTON_V2_ROLLBACK_ID, priorFailedChecks: ["H1"], priorRollbackVerified: true, previousDraftApprovalId: "draft-approval", h1NormalizationRepairCommit: HOUSTON_H1_NORMALIZATION_REPAIR_COMMIT, seoContractVersion: "projectorenclosure-public-seo-v1", publicVerifierVersion: HOUSTON_PUBLIC_VERIFIER_VERSION })).toMatchObject({ attempt: "V3", priorFailedCheck: "H1", h1NormalizationRepairCommit: HOUSTON_H1_NORMALIZATION_REPAIR_COMMIT }));
  test.each([{ priorIntentId: "wrong" }, { priorFailedChecks: ["META"] }, { priorRollbackVerified: false }, { h1NormalizationRepairCommit: "stale" }])("rejects stale V3 retry authority", (patch) => expect(() => freezeHoustonRetryV3Authority({ priorApprovalId: HOUSTON_V2_APPROVAL_ID, priorIntentId: HOUSTON_V2_INTENT_ID, priorPublicationReceiptId: HOUSTON_V2_RECEIPT_ID, priorFailureId: HOUSTON_V2_FAILURE_ID, priorRollbackId: HOUSTON_V2_ROLLBACK_ID, priorFailedChecks: ["H1"], priorRollbackVerified: true, previousDraftApprovalId: "draft-approval", h1NormalizationRepairCommit: HOUSTON_H1_NORMALIZATION_REPAIR_COMMIT, seoContractVersion: "projectorenclosure-public-seo-v1", publicVerifierVersion: HOUSTON_PUBLIC_VERIFIER_VERSION, ...patch })).toThrow("HOUSTON_V3_RETRY_AUTHORITY_STALE"));
  test("binds owner publication approval to exact draft 13093 and certified theme state", () => expect(freeze()).toMatchObject({ wordpressObjectId: "13093", bodyHash: HOUSTON_APPROVED_BODY_HASH, seoHash: HOUSTON_APPROVED_SEO_HASH, themeCertificationId: "theme-cert", previewDraftDrift: "MINOR" }));
  test.each([["object", { wordpressObjectId: "13094" }], ["body", { bodyHash: "f".repeat(64) }], ["SEO", { seoHash: "e".repeat(64) }], ["template", { template: "default" }]])("rejects stale %s authority", (_label, patch) => expect(() => freeze({ readback: { ...readback, ...patch } })).toThrow("HOUSTON_PUBLICATION_APPROVAL_IDENTITY_STALE"));
  test("requires theme integration and all four responsive captures", () => { expect(() => freeze({ certification: { ...certification, themeIntegrationReady: false } })).toThrow(); expect(() => freeze({ certification: { ...certification, captures: certification.captures.slice(1) } })).toThrow(); });
  test("requires every readiness gate and rejects unsafe claims or preview links", () => { const identity = freeze(); expect(evaluateHoustonPublicationReadiness({ html: "<h1>Houston</h1>", identity, linksValid: true, mediaValid: true, themeReady: true, ownerApproved: true }).failures).toEqual([]); expect(evaluateHoustonPublicationReadiness({ html: "<h1>Our Houston office</h1><a href='http://localhost:3003/glw/preview'>x</a>", identity, linksValid: true, mediaValid: true, themeReady: false, ownerApproved: true }).failures).toEqual(expect.arrayContaining(["CONTENT_READY", "LINKS_READY", "CLAIM_SAFE", "THEME_INTEGRATION_READY", "PUBLICATION_READY"])); });
  test("uses exact Houston SEO authority and removes the generic meta-length rule", () => { const contract = resolveHoustonPublicSeoContract(); expect(contract.metaDescription).toMatchObject({ state: "EXPECTED_NONEMPTY_NORMALIZED", expectedValue: expect.stringContaining("Houston commercial AV") }); expect(evaluate().failedChecks).toEqual([]); expect(readFileSync(join(process.cwd(), "src/modules/glw/houston-reference-page-publication.ts"), "utf8")).not.toContain("metaDescription.length > 20"); });
  test.each(["Protected projection for Houston’s indoor and covered commercial spaces.", "Protected projection for Houston&#8217;s indoor and covered commercial spaces.", "Protected projection for Houston&#x2019;s indoor and covered commercial spaces.", "Protected projection for Houston&amp;#8217;s indoor and covered commercial spaces.", "Protected projection for Houston's indoor and covered commercial spaces."])("normalizes equivalent public H1 representation %s", (raw) => { const result = evaluate(probe({ visibleH1Texts: [raw], normalizedH1Texts: [normalizeHoustonSemanticText(raw)] })); expect(result.checks.find((item) => item.code === "H1")?.state).toBe("PASS"); });
  test("does not hide a genuine H1 semantic mismatch", () => { const raw = "Protected projection for Austin's outdoor spaces."; const result = evaluate(probe({ visibleH1Texts: [raw], normalizedH1Texts: [normalizeHoustonSemanticText(raw)] })); expect(result.failedChecks).toContain("H1"); });
  test.each([
    ["EXPECTED_EMPTY", "", "PASS"],
    ["EXPECTED_EMPTY", "unexpected", "FAIL"],
    ["EXPECTED_NONEMPTY_EXACT", "Exact", "PASS"],
    ["EXPECTED_NONEMPTY_EXACT", " exact ", "FAIL"],
    ["EXPECTED_NONEMPTY_NORMALIZED", "  Exact   value ", "PASS"],
    ["OPTIONAL", "", "PASS"],
  ])("evaluates %s meta authority", (state, actual, expected) => { const contract: HoustonPublicSeoContract = { ...resolveHoustonPublicSeoContract(), metaDescription: { state: state as never, expectedValue: state === "EXPECTED_EMPTY" || state === "OPTIONAL" ? null : state === "EXPECTED_NONEMPTY_EXACT" ? "Exact" : "Exact value" } }; expect(evaluate(probe({ metaDescription: actual, metaDescriptionPresent: true }), contract).checks.find((item) => item.code === "META")?.state).toBe(expected); });
  test("fails missing required meta but supports missing evidence as NOT_EVALUATED", () => { expect(evaluate(probe({ metaDescription: null, metaDescriptionPresent: false })).failedChecks).toContain("META"); const unknown = evaluate(probe({ metaDescription: null, metaDescriptionPresent: null })); expect(unknown.checks.find((item) => item.code === "META")?.state).toBe("NOT_EVALUATED"); expect(unknown.overallState).toBe("BLOCKED"); });
  test("keeps certified Dallas compatible with empty public meta", () => { const title = "Fan Cooled Projector Enclosures in Dallas - Projector Enclosures - Screen Solutions International"; const canonical = "https://projectorenclosure.com/fan-cooled-projector-enclosures/texas/dallas/"; const checks = evaluatePublicSeoContract({ contract: resolveDallasCertifiedSeoContract({ publicTitle: title, publicCanonical: canonical }), actual: { title, canonical, metaDescription: null, indexability: "INDEXABLE" } }); expect(checks.every((item) => item.state === "PASS")).toBe(true); });
  test("persists complete HTTP, redirect, DOM, and safe-header probe evidence without secrets", async () => { const html = `<html><head><title>Fan Cooled Projector Enclosures Houston | ProjectorEnclosure.com</title><link rel="canonical" href="${HOUSTON_PUBLIC_URL}"><meta name="description" content="Plan fan cooled projector enclosures for Houston commercial AV, covered event, and convention spaces with project-specific exposure review."><meta name="robots" content="index, follow"></head><body data-market-primary="COMMERCIAL_AV"><h1>Protected projection for Houston&#8217;s indoor and covered commercial spaces.</h1>${HOUSTON_MEDIA_ROLES.map((role) => `<img data-media-role="${role}">`).join("")}</body></html>`; const fetcher = jest.fn<typeof fetch>().mockResolvedValueOnce(new Response(null, { status: 302, headers: { location: `${HOUSTON_PUBLIC_URL}?token=secret`, "set-cookie": "secret=value" } })).mockResolvedValueOnce(new Response(html, { status: 200, headers: { "content-type": "text/html", server: "Apache", "set-cookie": "secret=value" } })); const collected = await collectHoustonFirstPublicProbe({ publicationIntentId: "intent", publicationReceiptId: "receipt", fetcher }); expect(collected.probe).toMatchObject({ httpStatus: 200, finalUrl: HOUSTON_PUBLIC_URL, title: expect.stringContaining("Houston"), metaDescriptionPresent: true, canonical: HOUSTON_PUBLIC_URL, indexability: "INDEXABLE", visibleH1Count: 1, visibleH1Texts: [expect.stringContaining("&#8217;")], normalizedH1Texts: ["Protected projection for Houston's indoor and covered commercial spaces."], expectedH1Normalized: "Protected projection for Houston's indoor and covered commercial spaces.", approvedContentMarkerPresent: true, semanticMediaRoles: HOUSTON_MEDIA_ROLES }); expect(collected.probe.redirectChain).toEqual([{ status: 302, location: HOUSTON_PUBLIC_URL, resolvedDestination: HOUSTON_PUBLIC_URL }]); expect(JSON.stringify(collected.probe)).not.toMatch(/set-cookie|authorization|secret=value|token=secret/i); });
  test("retains every failed check and classifies SEO-only failures", () => { const result = evaluate(probe({ title: "Wrong", metaDescription: "Wrong", robots: "noindex", indexability: "NOINDEX" })); expect(result.failedChecks).toEqual(expect.arrayContaining(["TITLE", "META", "ROBOTS"])); expect(result.primaryFailureClassification).toBe("SEO_FAILURE"); expect(result.checks).toHaveLength(16); });
  test("classifies missing evidence as blocked and content mismatch as immediate rollback", () => { expect(evaluate(probe({ httpStatus: null, finalUrl: null, title: null, metaDescription: null, metaDescriptionPresent: null, canonical: null, indexability: "NOT_EVALUATED", visibleH1Count: null, approvedContentMarkerPresent: null, devLeak: null, previewLeak: null }), resolveHoustonPublicSeoContract()).primaryFailureClassification).toBe("VERIFICATION_BLOCKED"); expect(evaluate(probe({ approvedContentMarkerPresent: false, bodyMarkerClassification: "CONTENT_MISMATCH" })).primaryFailureClassification).toBe("IMMEDIATE_ROLLBACK_REQUIRED"); });
  test("offline Houston evidence does not manufacture a pass when meta is unknown", () => { const result = evaluate(probe({ metaDescription: null, metaDescriptionPresent: null })); expect(result.checks.find((item) => item.code === "META")?.state).toBe("NOT_EVALUATED"); expect(result.overallState).toBe("BLOCKED"); expect(result.failedChecks).not.toContain("META"); });
  test("preserves Commercial AV, supporting applications, climate guidance, links, and media", () => { const valid = { approvedBodyHash: HOUSTON_APPROVED_BODY_HASH, postPublishBodyHash: HOUSTON_APPROVED_BODY_HASH, expectedRoles: HOUSTON_MEDIA_ROLES, publicRoles: HOUSTON_MEDIA_ROLES, expectedLinks: ["https://projectorenclosure.com/a"], publicLinks: ["https://projectorenclosure.com/a"], publicHtml: "Commercial AV EVENT VENUE PROJECTION MAPPING covered humidity climate-controlled" }; expect(evaluateHoustonDraftPublicDrift(valid).classification).toBe("MINOR"); expect(evaluateHoustonDraftPublicDrift({ ...valid, publicRoles: valid.publicRoles.slice(1) }).classification).toBe("CRITICAL"); expect(evaluateHoustonDraftPublicDrift({ ...valid, publicHtml: "Houston" }).classification).toBe("MATERIAL"); });
  test("persists evidence and full failure before rollback with exact timing phases", () => { const service = readFileSync(join(process.cwd(), "src/modules/glw/houston-reference-page-publication-service.ts"), "utf8"); const probeIndex = service.indexOf('saveHoustonPublicationRecord("firstProbes"'); const resultIndex = service.indexOf('saveHoustonPublicationRecord("verifierResults"'); const failureIndex = service.indexOf('saveHoustonPublicationRecord("failures"'); const rollbackIndex = service.indexOf('await restoreDraft'); expect(probeIndex).toBeGreaterThan(0); expect(resultIndex).toBeGreaterThan(probeIndex); expect(failureIndex).toBeGreaterThan(resultIndex); expect(rollbackIndex).toBeGreaterThan(failureIndex); for (const phase of ["PUBLISH_STARTED", "PUBLISH_CONFIRMED", "FIRST_PROBE_STARTED", "FIRST_PROBE_COMPLETED", "VERIFICATION_DECIDED", "ROLLBACK_STARTED", "ROLLBACK_COMPLETED"]) expect(service).toContain(`"${phase}"`); });
  test("stores probe, full verifier result, failure, timings, and rollback append-only", async () => { const { mkdtempSync, rmSync } = await import("node:fs"); const { tmpdir } = await import("node:os"); const root = mkdtempSync(join(tmpdir(), "houston-verifier-")); const original = process.env.GCP_FOUNDATION_PERSISTENCE_DIR; process.env.GCP_FOUNDATION_PERSISTENCE_DIR = root; jest.resetModules(); const repository = await import("../houston-reference-page-publication-repository"); const firstProbe = probe({ metaDescription: "wrong", metaDescriptionPresent: true }); const result = evaluate(firstProbe); const failure = { failureId: "failure", reason: "HOUSTON_PUBLIC_VERIFICATION_FAILED", probeId: firstProbe.probeId, verifierResultId: result.verifierResultId, failedChecks: result.failedChecks, primaryFailureClassification: result.primaryFailureClassification!, rollbackRequired: true, failedAt: result.decisionAt }; repository.saveHoustonPublicationRecord("firstProbes", firstProbe, "FIRST_PUBLIC_PROBE_PERSISTED", firstProbe.completedAt); repository.saveHoustonPublicationRecord("verifierResults", result, "PUBLIC_VERIFIER_RESULT_PERSISTED", result.decisionAt); repository.saveHoustonPublicationRecord("failures", failure, "PUBLIC_VERIFICATION_FAILED", failure.failedAt); for (const [index, phase] of ["PUBLISH_STARTED", "PUBLISH_CONFIRMED", "FIRST_PROBE_STARTED", "FIRST_PROBE_COMPLETED", "VERIFICATION_DECIDED", "ROLLBACK_STARTED", "ROLLBACK_COMPLETED"].entries()) repository.saveHoustonPublicationRecord("publicationTimings", { timingId: `timing-${index}`, publicationIntentId: "intent", phase: phase as never, at: `2026-09-13T20:28:${String(index).padStart(2, "0")}.000Z` }, `TIMING_${phase}`, `2026-09-13T20:28:${String(index).padStart(2, "0")}.000Z`); repository.saveHoustonPublicationRecord("rollbacks", { rollbackId: "rollback", publicationIntentId: "intent", failureId: "failure", startedAt: "2026-09-13T20:28:05.000Z", completedAt: "2026-09-13T20:28:06.000Z", wordpressRestoredToDraft: true, bodyHash: HOUSTON_APPROVED_BODY_HASH, seoHash: HOUSTON_APPROVED_SEO_HASH }, "PUBLICATION_ROLLED_BACK", "2026-09-13T20:28:06.000Z"); const state = repository.getHoustonPublicationState(); expect(state.firstProbes[0]).toMatchObject({ httpStatus: 200, finalUrl: HOUSTON_PUBLIC_URL, title: expect.any(String), canonical: HOUSTON_PUBLIC_URL, robots: "index, follow", visibleH1Count: 1, approvedContentMarkerPresent: true }); expect(state.verifierResults[0].failedChecks).toEqual(["META"]); expect(state.failures[0]).toMatchObject({ failedChecks: ["META"], primaryFailureClassification: "SEO_FAILURE" }); expect(state.publicationTimings.map((item) => item.phase)).toHaveLength(7); expect(state.audit.map((item) => item.action)).toEqual(["FIRST_PUBLIC_PROBE_PERSISTED", "PUBLIC_VERIFIER_RESULT_PERSISTED", "PUBLIC_VERIFICATION_FAILED", ...["PUBLISH_STARTED", "PUBLISH_CONFIRMED", "FIRST_PROBE_STARTED", "FIRST_PROBE_COMPLETED", "VERIFICATION_DECIDED", "ROLLBACK_STARTED", "ROLLBACK_COMPLETED"].map((phase) => `TIMING_${phase}`), "PUBLICATION_ROLLED_BACK"]); if (original === undefined) delete process.env.GCP_FOUNDATION_PERSISTENCE_DIR; else process.env.GCP_FOUNDATION_PERSISTENCE_DIR = original; rmSync(root, { recursive: true, force: true }); });
  test("contains one exact Houston publication and prohibits scheduler, jobs, executions, and San Antonio", () => { const service = readFileSync(join(process.cwd(), "src/modules/glw/houston-reference-page-publication-service.ts"), "utf8"); const route = readFileSync(join(process.cwd(), "src/app/api/glw/houston-approved-draft/route.ts"), "utf8"); expect((service.match(/publishGenesisWordPressDraft\(/g) ?? []).length).toBe(1); expect(service).toContain('wordpressObjectId: HOUSTON_WORDPRESS_OBJECT_ID'); expect(`${service}\n${route}`).not.toMatch(/leaseGlwCampaignTargets|dispatchGlwCampaign|createGlwPageExecution|n8n|san-antonio/i); for (const marker of ["schedulerInvoked: false", "dispatchPerformed: false", "leaseCreated: false", "jobCreated: false", "executionCreated: false", "newDispatchDateCreated: false"]) expect(route).toContain(marker); });
});