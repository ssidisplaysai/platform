jest.mock("server-only", () => ({}));
import { readFileSync } from "node:fs";
import { NAVIGATION_REPLACEMENTS, transformProjectorEnclosureNavigation } from "../projectorenclosure-global-navigation-repair";

test("applies only approved URL replacements present in a widget", () => { const source = `<nav><a href="/homeline-series/">Homeline</a><a href="https://projectorenclosure.com/request-a-quote/">Quote</a><a href="/unchanged/">Other</a></nav>`; const result = transformProjectorEnclosureNavigation({ html: source, footer: false }); expect(result.applied["/homeline-series/"]).toBe(1); expect(result.applied["/request-a-quote/"]).toBe(1); expect(result.html).toContain("/homeline-projector-enclosure/"); expect(result.html).toContain("/contact-us-projection-enclosure/"); expect(result.html).toContain("/unchanged/"); expect(result.html).not.toContain("/homeline-series/"); });
test("footer legal links keep visible text and lose anchor behavior", () => { const result = transformProjectorEnclosureNavigation({ html: `<footer><a class="legal" href="/privacy-policy/"><strong>Privacy Policy</strong></a> | <a href="/terms/">Terms</a></footer>`, footer: true }); expect(result.legal["/privacy-policy/"]).toEqual({ occurrences: 1, textPreserved: true, clickable: false }); expect(result.legal["/terms/"]).toEqual({ occurrences: 1, textPreserved: true, clickable: false }); expect(result.html).toContain("<strong>Privacy Policy</strong>"); expect(result.html).not.toMatch(/href="\/(?:privacy-policy|terms)\//); });
test("approved replacement set is exact", () => { expect(NAVIGATION_REPLACEMENTS).toHaveLength(8); expect(NAVIGATION_REPLACEMENTS.map(([oldHref]) => oldHref)).toEqual(["/homeline-series/", "/outdoor-entertainment/", "/education/", "/houses-of-worship/", "/museums-exhibits/", "/stadiums-arenas/", "/government-military/", "/request-a-quote/"]); });
test("repair source includes drift-stop and reverse-order dual rollback guards", () => { const source = readFileSync("src/modules/foundation/projectorenclosure-global-navigation-repair.ts", "utf8"); expect(source).toContain("PROJECTORENCLOSURE_GLOBAL_NAVIGATION_AUTHORITY_DRIFT"); expect(source).toContain("[...writes].reverse()"); expect(source).toContain("PROJECTORENCLOSURE_GLOBAL_NAVIGATION_ROLLBACK_HASH_DRIFT"); expect(source).toContain("PROJECTORENCLOSURE_GLOBAL_NAVIGATION_ROLLBACK_VERIFICATION_FAILED"); });