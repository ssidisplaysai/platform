# Recovery Hardening

Recovery still fails closed for corrupt manifests, schema mismatches, tenant mismatches, and invariant violations. The restart suite validates successful recovery and negative-path resilience.
