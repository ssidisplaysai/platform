# Distributed Atomicity and Reconciliation

The integrated evidence includes successful commit paths and a reconciliation-required path when external acceptance diverges from local commit behavior. Replay remains idempotent and conflicts remain fail-closed.
