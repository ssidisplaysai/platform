# 13 Recovery Invariants

Recovery validation covers cross-entity relationships, quantity invariants, idempotency consistency, and tenant scope integrity.

Structural validation remains separate from domain invariant validation so malformed storage and invalid domain state are rejected for different reasons.