# 06 Approval and Signature Model

Approval model:

- finite transition model
- transition history with actor attribution
- invalid transitions fail-closed

Signature model:

- signature capture with signer metadata
- revocation lifecycle with reason capture
- active vs revoked signature accounting in metrics
