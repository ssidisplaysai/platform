# 04 Template Model

Template model capabilities:

- tenant-scoped templates
- versioned template metadata
- format-aware templates (HTML, DOCX, PDF)
- deterministic variable replacement rendering

Ownership note:

- Template rendering is document-owned generation behavior.
- Binary outputs remain Asset platform owned storage.
