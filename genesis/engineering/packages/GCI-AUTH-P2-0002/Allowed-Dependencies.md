# Allowed Dependencies

The Entity Runtime may depend only on approved upstream contracts and outputs from:
- Runtime Foundation;
- Evidence Runtime;
- Evidence Validation Runtime;
- Manifest Runtime;
- Replay Runtime;
- IBR Runtime.

Preferred imports must be the narrowest contract-only surfaces necessary to support deterministic entity canonicalization.

Any additional dependency requires a separate governance action.