# 13 Test Design

Focused S7 tests cover:
- Registry deterministic behavior and duplicate rejection
- Mandatory product validator fail-closed behavior
- Optional reference handling when validator is missing
- Optional reference success path when validator is present
- Runtime registration exposure for reference validation service
- Health and metrics snapshot behavior
