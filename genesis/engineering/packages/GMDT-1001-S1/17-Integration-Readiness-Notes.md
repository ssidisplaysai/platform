# 17 Integration Readiness Notes

Ready for next slices:
- Runtime composition can consume stable contracts/domain helpers.
- Persistence adapters can enforce optimistic concurrency using expected-version primitives.
- Command handlers can attach idempotency records and trace append-only checks.

Deferred to later slices:
- API/integration transports.
- Mission-control telemetry wiring.
