# Genesis Routing Event Model

Published events:
- RoutingCreated
- RoutingReleased
- RoutingUpdated
- RoutingVersionCreated
- RoutingArchived
- RoutingClosed
- RoutingRevised

Event contract fields include event ID, contract version, aggregate type, aggregate ID, aggregate version, correlation ID, causation ID, timestamp, actor, organization, payload, and metadata.

Result: PASS