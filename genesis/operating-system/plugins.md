# Plugins

Plugins provide extension points for Genesis OS without weakening the platform core.

## Guidelines

- Keep plugin contracts explicit.
- Allow plugin lifecycle integration through the runtime.
- Preserve modularity and avoid hardwired dependencies.
