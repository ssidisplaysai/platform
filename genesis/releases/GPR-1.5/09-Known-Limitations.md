# 09 Known Limitations

Known limitations carried forward from the certified baseline:
- The notification foundation continues to use the baseline file-backed and in-memory runtime model.
- The untracked runtime `data/` directory remains outside the release package.
- No new platform capability is introduced by GPR-1.5.
