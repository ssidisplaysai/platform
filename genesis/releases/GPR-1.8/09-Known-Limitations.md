# 09 Known Limitations

Known release limitations:

- Release packaging does not re-run implementation changes; it certifies an existing frozen baseline.
- Runtime data directories are intentionally excluded from repository tracking.

No blocking limitations were identified for release documentation certification.
